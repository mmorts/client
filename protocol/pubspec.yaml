name: protocol
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com
# author: Issac <tejainece@gmail.com>

environment:
  sdk: '>=2.2.0 <3.0.0'

dependencies:
  spec:
    path: ../spec
  pathing:
    path: ../pathing
  pos:
    path: ../pos

dev_dependencies:
  pedantic: ^1.0.0
  test: ^1.0.0
